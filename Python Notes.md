- To display a text , print("hello world").
- If we are writing a script in python at the beginning write !#/bin/python3.
- to run the script , python3 file name or ./filename.
- math can be simply done print(a +b) PEMDAS.
- To create a variable name = "text".
- To find the character number len().
- To create a if else condition: 
	if condition:
		return 
	elif condition:
		return
	else:
		return
NOTE: you can concatenate string with integer. 